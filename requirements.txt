# --- Framework web ---
fastapi
uvicorn[standard]

# --- Configuración y validación ---
pydantic
pydantic-settings

# --- Subida de archivos (Form / UploadFile) ---
python-multipart

# --- Plantillas del panel de administración (app/templates) ---
jinja2

# --- Cliente HTTP asíncrono para la API de Extend ---
httpx

# --- Base de datos PostgreSQL (API keys y clasificaciones) ---
psycopg2-binary

# --- Caché centralizada compartida entre los workers de gunicorn ---
redis

# --- Cliente HTTP para el script de prueba ---
requests
