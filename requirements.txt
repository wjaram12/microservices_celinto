
# --- Framework web ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
# Servidor de producción (Linux). En Windows usar uvicorn con --workers.
gunicorn==23.0.0

# --- Configuración y validación ---
pydantic==2.13.4
pydantic-settings==2.14.1

# --- Base de datos PostgreSQL (API keys, clasificaciones) ---
psycopg2-binary==2.9.12

# --- Caché centralizada, compartida entre los workers de gunicorn ---
redis==8.0.0

# --- Cliente HTTP para los scripts de prueba (probar_*.py) ---
requests==2.34.2

# =============================================================================
# Clasificador documental (app/)
# =============================================================================

# --- Subida de archivos (Form / UploadFile) ---
python-multipart==0.0.32

# --- Plantillas del panel de administración (app/templates) ---
jinja2==3.1.6

# --- Cliente HTTP asíncrono para la API de Extend ---
httpx==0.28.1

# --- Reintentos de fallos transitorios (red/429/5xx) en las llamadas a Extend ---
tenacity==9.1.4

# =============================================================================
# Consulta de títulos SENESCYT (consulta_titulos/)
# =============================================================================

# --- Scraper portable del portal SENESCYT (JSF/PrimeFaces) ---
beautifulsoup4==4.15.0
soupsieve==2.8.4
urllib3==2.7.0

# --- OCR del captcha (resuelve la imagen sin intervención humana) ---
# ddddocr arrastra onnxruntime/opencv/numpy; se fijan para evitar sorpresas.
ddddocr==1.6.1
onnxruntime==1.27.0
opencv-python==4.13.0.92
numpy==2.4.6

# --- Generación de las imágenes de captcha en el servidor mock (solo desarrollo) ---
pillow==12.2.0

# =============================================================================
# Google Workspace (google_services/)
# =============================================================================

# google-api-python-client arrastra google-auth-httplib2, httplib2 y uritemplate.
google-auth==2.53.0
google-api-python-client==2.198.0
