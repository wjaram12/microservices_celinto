# Versiones fijadas a las probadas, para reproducibilidad en producción.

# --- Framework web (servicio de consulta y servidor mock de desarrollo) ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
# Servidor de producción (Linux). En Windows usar uvicorn con --workers.
gunicorn==23.0.0

# --- Configuración y validación ---
pydantic==2.13.4
pydantic-settings==2.14.1

# --- Núcleo de caché: Redis con TTL nativo (SETEX) ---
redis==8.0.0

# --- API keys: validación contra la tabla `api_keys` (PostgreSQL compartido) ---
psycopg2-binary==2.9.12

# --- Scraper portable del portal SENESCYT (JSF/PrimeFaces) ---
requests==2.34.2
beautifulsoup4==4.15.0
soupsieve==2.8.4
urllib3==2.7.0

# --- OCR del captcha (resuelve la imagen sin intervención humana) ---
# ddddocr arrastra onnxruntime/opencv/numpy; se fijan para evitar sorpresas.
ddddocr==1.6.1
onnxruntime==1.27.0
opencv-python==4.13.0.92
numpy==2.4.6

# --- Generación de las imágenes de captcha en el servidor mock (solo desarrollo) ---
pillow==12.2.0
