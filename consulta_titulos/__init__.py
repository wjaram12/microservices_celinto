"""App de consulta de títulos SENESCYT (paquete). Reutiliza la infraestructura
compartida de `commons` (DB, Redis, API keys) y aporta lo propio: scraper con OCR,
fuente, núcleo de caché por cédula y la API HTTP."""
